#include <iostream>
using namespace std;

class BubbleSort {
public:
    void bubbleSorted(int arr[], int size);
    void bubbleData();
    void display();
    int arr[50];
    int array_size;
    int temp;

};

void BubbleSort::bubbleData() 
{
    cout<<"Enter size of list:" << endl;
    cin>>array_size;
    cout<<"Enter elements" << endl;
    cout << endl;
    for(int i = 0; i <= array_size -1; i++) 
	{
        cin>>arr[i];
    }
     cout << "Elements of your unsorted list: ";
    for(int j = 0; j <= array_size -1; j++)
	{
        cout << arr[j] << " " ;
    }
    cout<<" "<<endl;
        display();
}



void BubbleSort::bubbleSorted(int arr[], int array_size)
 {
    for (int k = 0; k < array_size; k++)
        for (int m = 0; m < array_size -k -1; m++)
            if (arr[m] > arr[m +1]){
                temp = arr[m];
                arr[m] = arr[m + 1];
                arr[m + 1] = temp;
      }
}

void BubbleSort::display() {

    bubbleSorted(arr,array_size);
        cout << "Elements of your sorted list:  ";
        for(int x = 0; x < array_size; x++) {
            cout << arr[x] << " ";
        }
}

int main () {
    BubbleSort s1;
    s1.bubbleData();
    return 0;
}
